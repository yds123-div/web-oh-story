# 11 — 端到端主线验收

**Parent spec:** `spec.md`（同目录）

**What to build:** 全链路收尾验证。本地起真实后端（10588）+ 前端（5173），关闭一切 mock，用真实流程从零现造数据走通核心创作主线：**新建项目 → 提交剧本 → AI 提取资产 → 管理/编辑资产 → 分镜 → 工作台提示词生成**。顺带处理跨页面的收尾问题（残留 mock 调用、门控边角、导航跳转断链等）。

**Blocked by:** 07 — 资产 AI 能力；08 — 分镜 CRUD；09 — 分镜图片与工作台视频链路（连同各自前置）

**Status:** ready-for-agent

- [ ] 本地后端 + 前端，MSW 关闭，从空数据库开始用真实流程现造数据（不导入种子数据）
- [ ] 主线全流程走通：新建项目 → 提交剧本 → AI 提取资产（真实文本模型）→ 编辑资产 → 分镜 CRUD → 工作台视频提示词生成
- [ ] 全程数据真实持久：中途刷新页面后流程可继续，不丢数据
- [ ] 图像/视频生成调用呈现预期失败态（key 未到位），原因清晰、可重试、不阻塞主线
- [ ] 主线各步之间导航跳转无断链、无对不存在接口的调用、无静默失败
- [ ] 前端测试套件整体通过（API client 契约测试 + 页面集成测试）
- [ ] 在本文件 `## Comments` 下留档验收记录：走的路径、验证点、发现并修复的收尾问题
