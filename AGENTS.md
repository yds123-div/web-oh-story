# AGENTS.md

## Agent skills

### Issue tracker

Issue 以本地 markdown 文件跟踪，位于仓库根 `.scratch/<feature-slug>/`。见 `docs/agents/issue-tracker.md`。

### Domain docs

单上下文布局：根目录 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
